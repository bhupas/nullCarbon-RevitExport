<UserControl x:Class="SCaddins.ExportSchedules.Views.ExportSchedulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SCaddins.ExportSchedules.Views"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Simple converter for true=visible, false=collapsed -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <!-- Define rows: Top (header), Selection Panel, DataGrid, Status, Bottom (options buttons) -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top section: Login controls and authentication info -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="nullCarbon LCA Export" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center"/>

            <!-- Buttons for Login and Sign Up -->
            <Button Grid.Column="1" 
                    Content="{Binding LoginButtonText}"
                    Margin="5"
                    Padding="8,2"
                    cal:Message.Attach="LoginCommand" />

            <Button Grid.Column="2" 
                    Content="Sign up"
                    Margin="5"
                    Padding="8,2"
                    cal:Message.Attach="SignUpCommand" />
        </Grid>

        <!-- Team and Building selection panel -->
        <Grid Grid.Row="1" Margin="10" Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVis}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" 
                   Content="Team:" 
                   Margin="0,0,5,0" 
                   VerticalAlignment="Center"/>

            <ComboBox Grid.Column="1" 
                      Margin="0,0,10,0"
                      ItemsSource="{Binding Teams}"
                      SelectedItem="{Binding SelectedTeam}"
                      DisplayMemberPath="Name"
                      IsEnabled="{Binding IsLoggedIn}"/>

            <Label Grid.Column="2" 
                   Content="Building:" 
                   Margin="0,0,5,0" 
                   VerticalAlignment="Center"/>

            <ComboBox Grid.Column="3" 
                      Margin="0,0,0,0"
                      ItemsSource="{Binding Buildings}"
                      SelectedItem="{Binding SelectedBuilding}"
                      DisplayMemberPath="Name">
            </ComboBox>
        </Grid>

        <!-- Middle section: DataGrid displaying schedules -->
        <DataGrid x:Name="Schedules"
                  Grid.Row="2"
                  Margin="5"
                  AutoGenerateColumns="False"
                  GridLinesVisibility="None">
            <DataGrid.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" 
                                 Color="{x:Static SystemColors.HighlightColor}"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" 
                                 Color="{x:Static SystemColors.HighlightTextColor}"/>
                <Style x:Key="ColumnElementStyle" TargetType="TextBlock">
                    <Setter Property="Margin" Value="1"/>
                </Style>
                <Style TargetType="{x:Type DataGridCell}">
                    <Style.Triggers>
                        <Trigger Property="DataGridCell.IsSelected" Value="True">
                            <Setter Property="Background" Value="#333333"/>
                            <Setter Property="BorderBrush" Value="#333333"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="{x:Type DataGridColumnHeader}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.AlternatingRowBackground>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlLightColorKey}}"/>
            </DataGrid.AlternatingRowBackground>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" 
                                         Color="{x:Static SystemColors.HighlightColor}"/>
                    </Style.Resources>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.VerticalGridLinesBrush>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.WindowFrameColorKey}}"/>
            </DataGrid.VerticalGridLinesBrush>

            <DataGrid.HorizontalGridLinesBrush>
                <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.WindowFrameColorKey}}"/>
            </DataGrid.HorizontalGridLinesBrush>

            <DataGrid.Columns>
                <!-- DataGridTemplateColumn for selection check box -->
                <DataGridTemplateColumn Header="Select" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <!-- Name column -->
                <DataGridTextColumn IsReadOnly="True"
                                    Header="Name"
                                    Binding="{Binding Schedule.RevitName}"/>
                <!-- Type column -->
                <DataGridTextColumn Header="Type"
                                    Binding="{Binding Schedule.Type}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status section -->
        <TextBlock Grid.Row="3" 
                   Text="{Binding StatusMessage}" 
                   Margin="5">
            <TextBlock.Foreground>
                <SolidColorBrush Color="{Binding IsLoading, Converter={StaticResource BoolToVis}, ConverterParameter=#0000FF}"/>
            </TextBlock.Foreground>
        </TextBlock>

        <!-- Progress indicator -->
        <ProgressBar Grid.Row="3" 
                     IsIndeterminate="{Binding IsLoading}" 
                     Height="2" 
                     Margin="5,0,5,0" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>

        <!-- Bottom section: Options and Export buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Height="Auto"
                    Width="Auto"
                    MinWidth="20"
                    Margin="5"
                    x:Name="Options"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Grid.Column="0">
                <Image Source="/SCaddins;component/Assets/gear.png" Stretch="None"/>
                <Button.ToolTip>
                    <Label>Options</Label>
                </Button.ToolTip>
            </Button>

            <Button Height="Auto"
                    Width="Auto"
                    MinWidth="20"
                    Margin="5"
                    x:Name="SelectExportDir"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Grid.Column="1">
                <Image Source="/SCaddins;component/Assets/folder.png" Stretch="None"/>
                <Button.ToolTip>
                    <Label>Select export directory</Label>
                </Button.ToolTip>
            </Button>

            <Label x:Name="ExportDir" Margin="5" Grid.Column="2"/>

            <Button x:Name="ExportOnline" 
                    IsEnabled="{Binding ExportOnlineIsEnabled}" 
                    Content="Export Online" 
                    Padding="8,2"
                    Margin="5" 
                    Grid.Column="3"
                    Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVis}}"/>

            <Button x:Name="Export" 
                    IsEnabled="{Binding ExportIsEnabled}" 
                    Content="{Binding ExportLabel}" 
                    Padding="8,2"
                    Margin="5" 
                    Grid.Column="4"/>
        </Grid>
    </Grid>
</UserControl>